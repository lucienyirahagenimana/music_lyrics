in this music lyrics finder project there is same packackage you may install and run first go it.
all things is worked on terminal or command prompt
1) install npm using this keyword " npm i "
2) and before run " npm run build " its allows to build all requirement
3) after type " npm run start " this is allows to lounch project
